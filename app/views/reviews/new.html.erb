<div class="container">
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "Submit", class: "btn btn-outline-dark btn-sm" %>
  <% end %>
</div>
